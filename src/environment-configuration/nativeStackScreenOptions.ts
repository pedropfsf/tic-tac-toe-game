import { NativeStackNavigationOptions } from "@react-navigation/native-stack";

const nativeStackScreenOptions: NativeStackNavigationOptions = {
  headerShown: false,
  animation: 'fade'
};

export default nativeStackScreenOptions;